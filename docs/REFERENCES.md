# REFERENCES

- Closed-form control with spike coding networks (SCN). PDF in `papers/`.
- Neural optimal feedback control with local learning rules (Bio-OFC). PDF in `papers/`.

Tip: cite these within code docstrings where equations are implemented (e.g., fast matrix `Ω_f=-D^T D`, factorization `F_k=G`, `Ω_k=-G W_y`).
